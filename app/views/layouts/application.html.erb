<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mentore</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css?family=Noto+Sans+JP:400,700,900&display=swap" rel="stylesheet">
  </head>

  <body>
    <header class="header">
      <div class="inner">
        <div class="nav">
          <div class="nav__left">
            <%= link_to image_tag("logo.png", class: :logo), root_path %>
          </div>
            <div class="nav__right">
            <% if user_signed_in? %>
             
              <%= search_form_for @p, url: prototypes_search_path do |f| %>
                <%= f.label :genre_id_eq, '#' %>
                <%= f.collection_select :genre_id_eq, Genre.all, :id, :name,  include_blank: '指定なし' %>
                <%= f.submit '検索', class: "push__btn" %>
                
   
              <% end %>

              <%= link_to "アプリ投稿", new_prototype_path, class: :nav__btn %>
              <%= link_to "面接カレンダー", new_prototype_path, class: :nav__btn %>
              <%= link_to "Bookシェア", new_book_path, class: :nav__btn %>
              <%= link_to "企業面接", new_prototype_path, class: :nav__btn %>
            </div>
            <% else %>              
            <div class="nav__right">
              <%= link_to "ログイン", new_user_session_path, class: :nav__btn %>
              <%= link_to "新規登録", new_user_registration_path, class: :nav__btn %>
              <%= link_to 'ゲストログイン（閲覧用）',  application_guest_sign_in_path, method: :post, class: :nav__btn %>
            </div>
            <% end %>
        </div>
      </div>
    </header>
     <%= yield %>
    <footer class="footer">
      <p class="copyright">Copyright © Mentore All rights reserved.</p>
    </footer>
  </body>
</html>
